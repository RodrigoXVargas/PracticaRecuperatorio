export class Rubro {
    id: number;
    denominacion: string;

    constructor(
      id: number = 0,
      denominacion: string = "",
    ) {
      this.id = id;;
      this.denominacion = denominacion;
    }
  }

